// Placeholder — Snag overwrites this file with a pairing token when it
// prepares the extension folder (Settings → Browser integration). Newer
// versions can also pair automatically, so repository/older folders recover
// after a single extension reload. Without a
// token the in-page panel falls back to opening Snag via snag:// links.
var SNAG_CONFIG = { token: '', ports: [] }
